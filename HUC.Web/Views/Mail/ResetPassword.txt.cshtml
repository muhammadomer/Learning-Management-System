@using ActionMailer.Net.Mvc
@model HUC.Web.App.Users.UserModel
@{
    Layout = null;
}

Hi, @Model.FirstName

Below is a link to reset your password.

@Url.AbsoluteAction("ResetPassword", "Auth", new { area = "", code = Model.ResetPasswordKey })